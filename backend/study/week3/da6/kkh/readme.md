# 🐾 동물 사운드 재생 시스템 과제

## 📚 학습 목표
- 클래스 상속 (`class Child(Parent)`) 구현
- 메서드 오버라이딩을 통한 다형성 실현  
- `super()` 함수를 활용한 부모 클래스 메서드 호출
- 클래스 변수와 인스턴스 변수의 차이점 이해
- 프로퍼티(`@property`) 활용한 캡슐화
- 사용자 입력 처리 및 실제 사운드 파일 재생

## 🎯 과제 개요
다양한 동물들의 소리를 재생하는 시스템을 구현합니다. 각 동물은 고유한 특성을 가지며, 기분(기쁠 때/화날 때)에 따라 다른 사운드 파일을 재생할 수 있는 대화형 프로그램을 만듭니다.

## 🏗️ 시스템 구조

### 1. Animal 클래스 계층 (상속 관계)
```
Animal (부모 클래스) - 기본 동물
├── Dog (개)
├── Cat (고양이)  
├── Tiger (호랑이)
└── Human (사람)
```

### 2. 유틸리티 함수
- `play_first_seconds()`: 사운드 파일의 처음 n초만 재생

## 📋 구현 요구사항

### 🔧 필요 라이브러리 설치
```bash
pip install pydub
```

### 🎵 유틸리티 함수
```python
from pydub import AudioSegment
from pydub.playback import play

def play_first_seconds(wav_path: str, seconds: float = 3.0):
    """특정 파일의 앞 n초만 재생하는 공통 유틸 함수"""
    sound = AudioSegment.from_wav(wav_path)
    ms = int(seconds * 1000)
    play(sound[:min(ms, len(sound))])
```

### 🐾 Animal 클래스 (부모 클래스)
**인스턴스 속성:**
- `__name`: 동물 이름 (private, name-mangling)
- `__age`: 나이 (private, 검증 필요)
- `_species`: 종 (protected 관례)
- `_happy_wav`: 기쁠 때 사운드 파일 경로
- `_angry_wav`: 화날 때 사운드 파일 경로

**메서드:**
- `__init__(name, species, age, happy_wav, angry_wav)`: 생성자
- `name` (property): 이름 읽기 전용 프로퍼티
- `age` (property + setter): 나이 읽기/쓰기 프로퍼티 (0 이상 검증)
- `info()`: 동물 정보 출력
- `move()`: 기본 움직임 동작 (오버라이딩 가능)
- `happy_sound(seconds=3.0)`: 기쁠 때 사운드 재생
- `angry_sound(seconds=3.0)`: 화날 때 사운드 재생

**구현 예시:**
```python
class Animal:
    def __init__(self, name: str, species: str, age: int,
                 happy_wav: str, angry_wav: str):
        # "private" 속성 (name-mangling)
        self.__name = name
        self.__age = 0   # 세터 통해 검증
        self.age = age   # 검증 거쳐 설정
        # "protected" 관례
        self._species = species
        # 사운드 파일 경로
        self._happy_wav = happy_wav
        self._angry_wav = angry_wav

    @property
    def name(self) -> str:
        return self.__name

    @property
    def age(self) -> int:
        return self.__age

    @age.setter
    def age(self, value: int):
        if not isinstance(value, int) or value < 0:
            raise ValueError("나이는 0 이상의 정수여야 합니다.")
        self.__age = value

    def info(self):
        print(f"[정보] 이름: {self.name}, 종: {self._species}, 나이: {self.age}")

    def move(self):
        print(f"{self._species}({self.name}) 이(가) 움직입니다...")

    def happy_sound(self, seconds: float = 3.0):
        print(f"{self._species}({self.name}) - 기쁠 때 소리 재생")
        play_first_seconds(self._happy_wav, seconds=seconds)

    def angry_sound(self, seconds: float = 3.0):
        print(f"{self._species}({self.name}) - 싸울 때 소리 재생")
        play_first_seconds(self._angry_wav, seconds=seconds)
```

### 🐕 Dog 클래스 (개)
**상속:** Animal 클래스 상속

**생성자:**
- 기본 종: "개"
- 기본 나이: 2세
- 사운드 파일: `dog_happy.wav`, `dog_angry.wav`

**구현 예시:**
```python
class Dog(Animal):
    def __init__(self, name: str, age: int = 2):
        super().__init__(name, "개", age,
                         happy_wav="dog_happy.wav",
                         angry_wav="dog_angry.wav")
```

### 🐱 Cat 클래스 (고양이)
**상속:** Animal 클래스 상속

**생성자:**
- 기본 종: "고양이"
- 기본 나이: 3세
- 사운드 파일: `cat_happy.wav`, `cat_angry.wav`

### 🐅 Tiger 클래스 (호랑이)
**상속:** Animal 클래스 상속

**생성자:**
- 기본 종: "호랑이"
- 기본 나이: 5세
- 사운드 파일: `tiger_happy.wav`, `tiger_angry.wav`

### 👤 Human 클래스 (사람)
**상속:** Animal 클래스 상속

**생성자:**
- 기본 종: "사람"
- 기본 나이: 25세
- 사운드 파일: `human_happy.wav`, `human_angry.wav`

**오버라이딩:**
- `move()`: 사람만의 특별한 이동 방식 구현

**구현 예시:**
```python
class Human(Animal):
    def __init__(self, name: str, age: int = 25):
        super().__init__(name, "사람", age,
                         happy_wav="human_happy.wav",
                         angry_wav="human_angry.wav")

    def move(self):  # 오버라이딩
        print(f"사람({self.name})이(가) 걸어갑니다...")
```

## 🖥️ 메인 프로그램 구현

### 대화형 인터페이스
```python
if __name__ == "__main__":
    # 동물원 딕셔너리 생성
    zoo = {
        "개": Dog("바둑이"),
        "고양이": Cat("나비"),
        "호랑이": Tiger("호돌이"),
        "사람": Human("민수"),
    }
    
    # 사용자 입력 받기
    who = input("대상? (개/고양이/호랑이/사람): ").strip()
    mood = input("기분? (happy/angry): ").strip().lower()
    secs = input("몇 초 재생할까요? (기본 3): ").strip()
    
    # 초 수 파싱
    try:
        seconds = float(secs) if secs else 3.0
    except:
        seconds = 3.0
    
    # 동물 객체 가져오기 및 동작 실행
    obj = zoo.get(who)
    if not obj:
        print("등록되지 않은 대상입니다.")
    else:
        obj.info()        # 정보 출력
        obj.move()        # 움직임 (오버라이딩 확인)
        
        # 기분에 따른 사운드 재생
        if mood == "happy":
            obj.happy_sound(seconds=seconds)
        elif mood == "angry":
            obj.angry_sound(seconds=seconds)
        else:
            print("알 수 없는 기분입니다.")
```

## 🎵 필요한 사운드 파일들

### 준비해야 할 WAV 파일들
- `dog_happy.wav`: 개 기쁠 때 (멍멍)
- `dog_angry.wav`: 개 화날 때 (으르렁)
- `cat_happy.wav`: 고양이 기쁠 때 (야옹)
- `cat_angry.wav`: 고양이 화날 때 (하악)
- `tiger_happy.wav`: 호랑이 기쁠 때
- `tiger_angry.wav`: 호랑이 화날 때 (으르렁)
- `human_happy.wav`: 사람 기쁠 때 (웃음소리)
- `human_angry.wav`: 사람 화날 때 (화난소리)

### 사운드 파일 준비 방법
1. **무료 사운드 사이트 활용**: Freesound.org, Zapsplat 등
2. **직접 녹음**: 휴대폰으로 간단히 녹음
3. **텍스트 음성 변환**: 온라인 TTS 서비스 활용
4. **더미 파일**: 짧은 무음 파일로 테스트

## 🧪 실행 예시

### 실행 화면
```
대상? (개/고양이/호랑이/사람): 개
기분? (happy/angry): happy
몇 초 재생할까요? (기본 3): 5

[정보] 이름: 바둑이, 종: 개, 나이: 2
개(바둑이) 이(가) 움직입니다...
개(바둑이) - 기쁠 때 소리 재생
[사운드 재생: dog_happy.wav의 처음 5초]
```

### 다양한 테스트 케이스
1. **기본 동작**: 각 동물별 정보 출력 및 사운드 재생
2. **오버라이딩 확인**: Human의 `move()` 메서드가 다르게 동작
3. **프로퍼티 검증**: 나이에 음수 입력 시 에러 발생
4. **캡슐화**: private 속성에 직접 접근 불가

## ✅ 평가 기준

### 필수 구현 사항 (70점)
- [ ] Animal 부모 클래스 정의
- [ ] Dog, Cat, Tiger, Human 자식 클래스 상속 구현
- [ ] `super()` 함수 활용한 생성자 호출
- [ ] `@property` 데코레이터 활용한 캡슐화
- [ ] Human 클래스에서 `move()` 메서드 오버라이딩
- [ ] 사용자 입력을 통한 대화형 인터페이스
- [ ] pydub를 활용한 실제 사운드 재생

### 추가 구현 사항 (30점)
- [ ] 입력 검증 및 에러 처리 강화
- [ ] 더 많은 동물 클래스 추가 (Bird, Fish 등)
- [ ] 동물별 고유 메서드 추가 (bark(), meow() 등)
- [ ] 사운드 재생 길이 조절 기능
- [ ] 반복 재생 기능 구현
- [ ] GUI 인터페이스 추가 (tkinter 등)

## 📁 파일 구조

### models.py
```python
"""
동물 사운드 시스템의 모든 클래스 정의
- Animal 부모 클래스
- Dog, Cat, Tiger, Human 자식 클래스들
- 유틸리티 함수들
"""
```

### main.py
```python
"""
프로그램 실행 및 사용자 인터페이스
"""
from models import Dog, Cat, Tiger, Human

# 메인 실행 코드
```

### sounds/ 폴더
```
sounds/
├── dog_happy.wav
├── dog_angry.wav
├── cat_happy.wav
├── cat_angry.wav
├── tiger_happy.wav
├── tiger_angry.wav
├── human_happy.wav
└── human_angry.wav
```

## 🚀 실행 방법

### 1. 라이브러리 설치
```bash
pip install pydub
```

### 2. 사운드 파일 준비
- `sounds/` 폴더에 필요한 WAV 파일들 배치

### 3. 프로그램 실행
```bash
python main.py
```

## 💡 구현 팁

1. **상속 구조**: 공통 기능은 부모 클래스에, 특별한 기능은 자식 클래스에
2. **캡슐화**: `__name`(private), `_species`(protected) 구분 활용
3. **프로퍼티**: `@property`로 안전한 속성 접근 구현
4. **오버라이딩**: Human의 `move()` 메서드로 다형성 체험
5. **에러 처리**: 파일 없음, 잘못된 입력 등 예외 상황 고려
6. **사운드 파일**: 짧고 명확한 사운드로 테스트 효과 극대화

## 🎯 학습 효과

### 상속의 실용성 체험
- 같은 메서드 호출 → 다른 동작 확인
- 코드 중복 없이 새로운 동물 추가 가능

### 캡슐화의 필요성 이해
- private 속성으로 데이터 보호
- property로 안전한 접근 제공

### 다형성의 활용
- 동일한 인터페이스로 다양한 객체 조작
- 런타임에 실제 타입에 따른 동작 실행

---
**🎯 실제 사운드와 함께하는 생동감 있는 상속 학습을 경험해보세요!**